name: palimpsest
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.1.2+3

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  archive: ^4.0.9
  xml: ^6.6.1
  file_picker: ^10.2.4
  just_audio: ^0.10.5
  audio_session: ^0.2.3
  path_provider: ^2.1.5
  # ONNX Runtime-backed Whisper. Uses the NNAPI execution provider on
  # Android to offload matrix math to the device NPU (Hexagon on Pixel,
  # Tensor on Galaxy). Mirrors iOS's WhisperKit / Core ML / ANE path.
  sherpa_onnx: ^1.13.1
  # Audio decoding is platform-native: MediaExtractor + MediaCodec on
  # Android (see `android/.../NativeAudioDecoder.kt`), `Process.run` on
  # desktop hitting a system ffmpeg. We used to depend on
  # `ffmpeg_kit_flutter_new*`, but its published x86_64 .aar crashes at
  # JNI_OnLoad on modern Android emulators and any Android 15+ device,
  # and the minimal fork hasn't been republished with the fix.
  just_audio_background: ^0.0.1-beta.17
  # Desktop backend for just_audio. On Windows/Linux (and macOS), routes
  # just_audio playback through libmpv via media_kit. On Android/iOS this
  # package is a no-op — just_audio's native backends keep handling things.
  just_audio_media_kit: ^2.0.6
  media_kit_libs_audio: ^1.0.5
  shared_preferences: ^2.5.5
  # Android+iOS PDF text extraction. We hit this only on mobile; desktop
  # keeps shelling out to Calibre for PDF/MOBI conversion via the
  # `importViaCalibre` path in LibraryStore.
  read_pdf_text: ^0.3.1
  # sha256 of the audio file is the cache key for the transcript cache —
  # re-aligning the same audiobook (or attaching it to a different book
  # record) skips the multi-minute Whisper pass entirely.
  crypto: ^3.0.6
  # ShaderBuilder + AnimatedSampler — drives our GLSL page-curl shader
  # (lib/reader/curl_page_view.dart). Renders the current page to a
  # texture, then runs `shaders/page_curl.frag` on it for the iBooks-style
  # cylindrical fold + lighting. Bidirectional (forward/backward) via a
  # `direction` uniform inside the shader.
  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # integration_test lets us boot the real Flutter app (so the curl
  # FragmentProgram actually compiles and runs) and grab the rendered
  # output to a PNG. Used by integration_test/curl_capture_test.dart.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # GLSL page-curl shaders — two slots picked by CurlPageView based on
  # spread vs single mode:
  #   - page_curl_card.frag   : rigid hinge rotation, used in spread mode
  #     (tablet / desktop landscape / foldable open) where the math has to
  #     play nicely with a static facing page.
  #   - page_curl_paper.frag  : cylindrical paper curl, used in single mode
  #     (phone, folded foldable) — the iBooks-style real-paper look.
  # Both share the same uniform layout so the Dart-side painter doesn't
  # branch. See lib/reader/curl_page_view.dart for the slot wiring.
  shaders:
    - shaders/page_curl_card.frag
    - shaders/page_curl_paper.frag

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
